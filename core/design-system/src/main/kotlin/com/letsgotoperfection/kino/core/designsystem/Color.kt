package com.letsgotoperfection.kino.core.designsystem

import androidx.compose.ui.graphics.Color

// Primary Colors
val Primary = Color(0xFF6366F1) // Indigo - Modern, Professional
val PrimaryVariant = Color(0xFF4F46E5)
val OnPrimary = Color(0xFFFFFFFF)

// Secondary Colors
val Secondary = Color(0xFF10B981) // Emerald - Success, Progress
val SecondaryVariant = Color(0xFF059669)
val OnSecondary = Color(0xFFFFFFFF)

// Accent Colors
val Accent = Color(0xFFF59E0B) // Amber - Notifications, Warnings
val OnAccent = Color(0xFFFFFFFF)

// Error Colors
val Error = Color(0xFFEF4444) // Red
val OnError = Color(0xFFFFFFFF)

// Background Colors
val Background = Color(0xFFF9FAFB) // Light Gray
val OnBackground = Color(0xFF111827) // Near Black

// Surface Colors
val Surface = Color(0xFFFFFFFF) // White
val OnSurface = Color(0xFF111827) // Near Black
val SurfaceVariant = Color(0xFFF3F4F6) // Light Gray
val OnSurfaceVariant = Color(0xFF6B7280) // Gray

// Text Colors
val TextPrimary = Color(0xFF111827) // Near Black
val TextSecondary = Color(0xFF6B7280) // Gray
val TextTertiary = Color(0xFF9CA3AF) // Light Gray

// Task Priority Colors
val PriorityHigh = Color(0xFFEF4444) // Red
val PriorityMedium = Color(0xFFF59E0B) // Amber
val PriorityLow = Color(0xFF10B981) // Green
val PriorityDefault = Color(0xFF6B7280) // Gray

// Status Colors
val Success = Color(0xFF10B981) // Green
val Warning = Color(0xFFF59E0B) // Amber
val Info = Color(0xFF3B82F6) // Blue

// Border Colors
val Border = Color(0xFFE5E7EB) // Light Gray
val BorderFocus = Color(0xFF6366F1) // Primary

// Shadow Colors
val Shadow = Color(0x14000000) // Black with 8% opacity

// Divider Colors
val Divider = Color(0xFFE5E7EB) // Light Gray

// Overlay Colors
val Overlay = Color(0x80000000) // Black with 50% opacity
val OverlayLight = Color(0x40000000) // Black with 25% opacity